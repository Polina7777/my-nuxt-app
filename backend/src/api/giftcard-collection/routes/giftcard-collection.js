'use strict';

/**
 * giftcard-collection router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::giftcard-collection.giftcard-collection');
